void main() {
  // 声明，默认值
  // bool a;
  // print(a);


  // assert 断言
  // var a = true;
  // assert(a);
  // var name = '';
  // assert(name.isEmpty);
  // assert(name.isNotEmpty);
  // var num = 0 / 0;
  // assert(num.isNaN);


  // 只有 true 对象才被认为是 true。 所有其他的值都是 flase。
  // String name = 'ducafecat';
  // if(name) {
  //   print('this is name');
  // }


  // 逻辑运算符

  // && 逻辑与
  // bool a = true;
  // bool b = true;
  // assert(a && b);

  // || 逻辑或
  // bool a = true;
  // bool b = false;
  // assert(a || b);

  // ! 逻辑非
  // bool a = true;
  // bool b = !a;
  // print(b);

  // 关系运算符
  // var a = 1;
  // var b = 2;

  // ==	等于
  // if(a == b) {}

  // !=	不等于
  // if(a != b) {}

  // >	大于
  // if(a > b) {}

  // >=	大于或等于
  // if(a >= b) {}
  
  // <	小于
  // if(a < b) {}

  // <=	小于或等于
  // if(a <= b) {}

}
